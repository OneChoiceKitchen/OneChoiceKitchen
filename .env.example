# API
PORT=3000
NODE_ENV=development
JWT_SECRET=change-me-to-a-long-random-string

# Payments (optional — mock mode used when unset)
STRIPE_SECRET_KEY=

# Database — SQLite file used by Prisma (relative to api/ working directory)
# DATABASE_URL=file:./dev.db
